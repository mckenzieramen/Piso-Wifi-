PISO WIFI — PASSWORD RESET EMAIL

The project now uses a custom server-side password-reset email flow.

The customer-facing reset page remains:
https://piso-wifi.pages.dev/reset-password.html

The actual password-reset email is sent by the PISO WIFI Google Apps Script mailer using the branded HTML template.

See:
PASSWORD-RESET-CUSTOM-EMAIL-SETUP.txt

Do not paste the HTML template into the Firebase Console email body. The HTML is already embedded in the revised Apps Script mailer.
