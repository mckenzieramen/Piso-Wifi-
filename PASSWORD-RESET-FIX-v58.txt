PISO WIFI v58 — PASSWORD RESET FIX

Root cause fixed:
The Cloudflare function was calling Apps Script without the required secret and resetLink, while Apps Script expected those values.

New flow:
Customer -> /api/password-reset -> Apps Script -> verify Client ID + Gmail -> Firebase generates reset link -> GmailApp sends branded HTML email -> reset-password.html

Apps Script must be redeployed from the separate v58 Apps Script package.
